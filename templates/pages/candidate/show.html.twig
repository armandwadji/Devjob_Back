{% extends "base.html.twig" %}


{% block title %} Devjob - détail candidat {{candidat.firstname}} {% endblock %}

{% block body %}
<pre>
    {{ dump(candidat) }}
</pre>
	<main class="container mt-5">
        <div class="row g-0  rounded overflow-hidden flex-md-row mb-4 h-md-250 position-relative">
            <div class="col p-4 d-flex flex-column position-static">
                <h3 class="mb-0 text-capitalize">{{candidat.firstname}}  {{candidat.lastname}} </h3>
                <strong class="d-inline-block mb-2 text-primary">{{candidat.email}}</strong>
                {# <p class="card-text mb-auto">Téléphone : {{candidat.telephone}} </p> #}
                <h4 class="card-text mb-auto"> Téléphone : {{candidat.telephone}} </h4>
                {# <p class="ms-3">{{app.user.firstname ~  ' ' ~ app.user.lastname}}</p> #}
                {# <a href="#" class="stretched-link ms-3 ">{{app.user.email}}</a>  #}
            </div>
            {# <div class="col-auto d-lg-block">
                <img class='rounded-circle' style="width: 100%; height: 100%; max-height: 250px; max-width: 250px; object-fit: contain " src="{{ vich_uploader_asset(app.user.company, 'imageFile') ? vich_uploader_asset(app.user.company, 'imageFile') : 'https://picsum.photos/250/250' }}" alt="Logo de {{app.user.company.name}}">
            </div> #}
        </div>
	</main>
{% endblock %}